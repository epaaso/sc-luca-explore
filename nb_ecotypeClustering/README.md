# Ecotype Clustering

`abund_clusters.ipynb` clusters patients from global coabundance outputs.
Membership CSVs and retained clustering figures are written to
`results/ecotype_clustering/` and feed the per-cluster graph, marker, and
CellPhoneDB phases.

```bash
jupyter lab nb_ecotypeClustering/abund_clusters.ipynb
```
